;; to be included in the complex package
(putOp 'project '(complex)
  (fn [x] (make-real (real-part x))))

;; to be included in the real package
(putOp 'project '(real)
  (fn [x] (let [multiplier 8388608
                num (
                make-rational

(defn can-drop
  "return whether x can be dropped a level in its type 'tower'"
  [x]

